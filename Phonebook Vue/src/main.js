// Import the createApp function from the Vue library
import { createApp } from 'vue';

// Import the root component of your application (App.vue)
import App from './App.vue';

// Create a new Vue app instance and mount it to the '#app' element in the HTML
createApp(App).mount('#app');
